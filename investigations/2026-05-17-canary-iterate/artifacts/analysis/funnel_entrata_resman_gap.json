{
 "cohort": "funnel+entrata+resman residual (adapters EXIST, never measured)",
 "size": 239,
 "prod_true_tier1": {
  "funnel": "5/89 (5%)",
  "entrata": "4/76 (5%)",
  "resman": "1/74 (1%)",
  "combined": "~3%"
 },
 "finding": "Adapters exist in codebase but for the residual-ledger-tagged set they almost never deliver Tier-1 in prod (mostly TIER_3_DOM/TIER_4_LLM fallback). Gap = detection/routing not sending these to the adapters OR adapters fire but fail. entrata: 6 got TIER_1_API_ENTRATA yet true-tier1 only 4 (fires but 0-real-units sometimes).",
 "next": "measurement canary on current stack (image 9954f0f, existing funnel/entrata/resman adapters + latest detector). If converts -> prod-vs-canary detection win to promote. If not -> concrete 239-prop adapter-fix target w/ per-platform breakdown.",
 "cohort_csv": "gs://jugnu-canary/property-list/cohort_fer.csv",
 "status": "QUEUED behind repli-oll canary l5w5t (single shared job)",
 "VALIDATED_2026-05-18": {
  "funnel/UDR": "NOT a new cluster \u2014 RealPage OLL via vanity host oll-leasing.loftliving.com/RP.Leasing.AppService.WebHost (DataDome+xyz-token, same as Category-D). Shipped OLL adapter ALREADY parses this shape (host-agnostic: Workflow/ActivityGroups body + rp.leasing.appservice marker). Gap = detection routes udr.com->funnel (HTML has funnelleasing); the OLL hop is JS-post-interaction, invisible to static detection. udr.com/api/internal/data = CMS/amenities red herring, NOT pricing.",
  "entrata/springriver": "False-positive: /Apartments/module/application_authentication/ is a tenant LOGIN link; real unit data elsewhere. Detection mis-tag, adapter correctly finds no widget.",
  "resman/covington": "myresman.com signature present but portal URL/format not found by adapter \u2014 detection/extraction gap.",
  "reframe": "These need DETECTION-ROUTING fixes, not 3 new adapters. funnel/UDR is the existing OLL cluster in disguise. Blind measurement canary would show failure without revealing cause \u2014 validate-first was correct.",
  "status": "User: do not proceed, wait for next instruction. No routing fix / no canary started."
 },
 "DEVTOOLS_VALIDATED": {
  "entrata/springriver": {
   "real_api": "GET https://<sub>.prospectportal.com/?module=check_availability&is_secure=1&property[id]=<pid>&action=view_unit_spaces&property_floorplan[id]=<fpid>&move_in_date=YYYY-MM-DD&occupancy_type=conventional&unit_count=N",
   "returns": "text/html fragment of unit spaces per floorplan; min_rent/max_rent/unit_count echoed in query",
   "platform": "Entrata ProspectPortal (prospectportal.com) \u2014 IS Entrata but a DIFFERENT surface than shipped EntrataAdapter (/Apartments/module/widgets/). The /application_authentication/ link was the false-positive distractor.",
   "protection": "Cloudflare (cf_clearance + __cf_bm) \u2014 NOT DataDome, NOT stateful. GET w/ URL params.",
   "tractability": "SERVER-SIDE: probe_get already has cost-gated Web-Unlocker escalation that auto-solves CF managed challenge (same as securecafe/#2 subset). repli360/securecafe-class, NOT OLL-class.",
   "build_recipe": "enumerate property[id]+floorplan ids from ProspectPortal floorplans page (id in slug e.g. a1-712595), then per-fp check_availability GET via probe_get(+WU) -> parse HTML unit-spaces fragment (repli360 str / entrata-WP parser pattern).",
   "verified": "springriver: property[id]=1209258 fp 712595 -> 3 units $1,291"
  },
  "resman/covington": {
   "real_source": "<client>.myresman.com/Portal/Applicants/Availability?a=<acct>&p=<guid>&moveInDate=&refreshPricing=true (SSR HTML page; embeds var unitTypes=[...]). verified: stewart.myresman.com a=1041 p=68f5dd84-230b-4dad-9a2e-9d18dfaccdc2",
   "protection": "NONE \u2014 not Cloudflare-fronted, no auth (per resman.py iter-13 note). Clean server-side GET.",
   "adapter": "ResManAdapter ALREADY EXISTS and parses exactly this (var unitTypes).",
   "gap": "a/p params are interaction-gated \u2014 marketing site (covingtonplace.com) static HTML only has stewart.myresman.com/Portal/Access/SignIn/CVGTNPLC (resident login). The Applicants/Availability?a=&p= URL is produced by clicking Check Availability. Detector/resolver never sees it statically.",
   "fix": "CTA-hop -> surfaces the Portal/Applicants/Availability?a=&p= URL -> existing ResManAdapter parses server-side. EASIEST of the 3 (no bot wall, adapter exists)."
  }
 },
 "UNIFYING_ROOT_CAUSE": "All 3 sub-clusters (and OLL canary 10%): the real unit-data source URL is behind an interaction/redirect (Check Availability) the STATIC detector/resolver never sees \u2014 marketing site exposes only a login link. CTA-hop (commit 4194257) is the common enabler. Tractability after CTA-hop: resman=easiest (adapter exists, no wall); funnel/UDR=OLL browser-intercept (DataDome 2nd wall->residential render); entrata=NEW ProspectPortal adapter (server-side GET+WU for CF)."
}