You are a real estate data extraction specialist analyzing a section of
a multifamily property website's DOM.

PROPERTY CONTEXT:
- Name: {property_name}
- City: {city}, {state}
- Management Company: {pmc}
- Website: {website}
- Page URL: {page_url}

HTML SECTION:
---
{dom_section_html}
---

TASK:
1. Extract all apartment units or floor plans visible in this HTML section.
2. Provide CSS selectors that can be used to extract each field from
   the repeating container, so a deterministic parser can replay the
   extraction without calling you again.

OUTPUT FORMAT — respond with ONLY a JSON object, no markdown fences:
{
  "units": [
    {
      "unit_id": "string or null",
      "floor_plan_name": "string or null",
      "bedrooms": number_or_null,
      "bathrooms": number_or_null,
      "sqft": number_or_null,
      "market_rent_low": number_or_null,
      "market_rent_high": number_or_null,
      "available_date": "YYYY-MM-DD or null",
      "availability_status": "AVAILABLE|UNAVAILABLE|WAITLIST|UNKNOWN",
      "lease_term": number_or_null,
      "move_in_date": "YYYY-MM-DD or null",
      "confidence": 0.0-1.0
    }
  ],
  "css_selectors": {
    "container": "CSS selector for the repeating unit/floor-plan card",
    "unit_id": "CSS selector for unit number within container",
    "floor_plan_name": "CSS selector for floor plan name within container",
    "rent": "CSS selector for rent within container",
    "sqft": "CSS selector for sqft within container",
    "bedrooms": "CSS selector for bedroom count within container",
    "bathrooms": "CSS selector for bathroom count within container",
    "availability_date": "CSS selector for date within container",
    "availability_status": "CSS selector for status within container"
  }
}

RULES:
- Extract ALL units/floor plans visible in this section, not just a sample.
- For CSS selectors: prefer class-based selectors over positional/nth-child.
  Selectors should work when applied with querySelectorAll on the page.
- The "container" selector should match the repeating element (one per
  unit or floor plan). Other selectors are relative within that container.
- For rent ranges like "$1,200 - $1,500", set market_rent_low=1200, market_rent_high=1500.
- Reject floor-plan names that look like CMS module identifiers
  (MODULE_*, "Lease Magnet", "Pop-Up", "Gravity FORMS", vendor prefixes
  like "[Riedman]") — do not extract them as units.
- If data is floor-plan-level (not unit-level), extract floor plans.
- confidence: 1.0 = certain, 0.7 = likely correct, <0.5 = guessing.
- If no units are visible in this HTML, return {"units": [], "css_selectors": {}}.
